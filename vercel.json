{
    "version": 2,
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/static-build",
        "config": {
          "distDir": "build"
        },
        "env": {
      "NPM_FLAGS": "--legacy-peer-deps"
    }
      }
    ],
    "env": {
      "CI": "false"
    },
    "buildCommand": "npm run build"
  }

